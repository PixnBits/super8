{
  "name": "super8",
  "version": "0.1.0-pre",
  "description": "Interface to Arduino controlling a Super 8 projector for digitizing film",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "lint": "eslint ./server --ignore-path .gitignore --ext .js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pixnbits",
  "license": "MIT",
  "dependencies": {
    "@serialport/parser-readline": "^2.0.2",
    "serialport": "^7.1.5"
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "eslint-config-amex": "^11.1.0"
  }
}
