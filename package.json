{
  "name": "super8",
  "version": "0.1.0-pre",
  "description": "Interface to Arduino controlling a Super 8 projector for digitizing film",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "nodemon -w server -x \"npm start\"",
    "dev:client": "parcel watch client/index.html --out-dir public",
    "build": "rimraf public && parcel build client/index.html --out-dir public",
    "lint": "eslint ./server --ignore-path .gitignore --ext .js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pixnbits",
  "license": "MIT",
  "dependencies": {
    "@serialport/parser-readline": "^2.0.2",
    "node-static": "^0.7.11",
    "serialport": "^7.1.5",
    "ws": "^7.1.2"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "eslint": "^6.5.1",
    "eslint-config-amex": "^11.1.0",
    "nodemon": "^1.19.3",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0"
  }
}
